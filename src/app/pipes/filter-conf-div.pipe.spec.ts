import { FilterConfDivPipe } from './filter-conf-div.pipe';

describe('FilterConfDivPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterConfDivPipe();
    expect(pipe).toBeTruthy();
  });
});
